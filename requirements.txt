# 株式検索システム - Python依存関係
# フレームワーク
Flask==2.3.3
Werkzeug==2.3.7

# データベース（SQLite3は標準ライブラリに含まれるため、追加パッケージ不要）

# Web開発関連
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# 日時処理
python-dateutil==2.8.2

# HTTPクライアント（外部API統合用 - 将来の拡張に備えて）
requests==2.31.0
urllib3==2.0.7

# 株価データ取得用
yfinance==0.2.28
pandas>=1.3.0

# J-Quants API（日本取引所公式API）
# jquants-api-client>=1.0.0  # 一時的にコメントアウト（環境問題のため）

# JSON処理の拡張（標準ライブラリで十分だが、パフォーマンス向上のため）
# simplejson==3.19.2  # オプション

# 開発・テスト用（本番環境では不要）
# pytest==7.4.3
# pytest-flask==1.3.0

# CORS対応（将来的にAPI専用サーバーとして使用する場合）
Flask-CORS==4.0.0

# セキュリティ（本番環境推奨）
# cryptography==41.0.7

# 環境変数管理
python-dotenv==1.0.0

# Renderデプロイ用
gunicorn==21.2.0

# ログ管理
# loguru==0.7.2  # オプション

# データ検証
# marshmallow==3.20.1  # オプション

# 型ヒント（開発時の品質向上）
# typing-extensions==4.8.0

# SQLite拡張（高度なクエリやパフォーマンス向上）
# sqlite-utils==3.35.1  # オプション
